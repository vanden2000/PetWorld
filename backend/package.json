{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently -k -n serve,vite -c blue,magenta \"php artisan serve\" \"vite\"",
        "dev:vite": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "concurrently": "^10.0.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    },
    "dependencies": {
        "quill": "^2.0.3"
    }
}
